name: Jest Tests

defaults:
  run:
    working-directory: app

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run Jest
        run:
          npm build
          npm test
